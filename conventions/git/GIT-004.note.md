This rule is enforced by `coding-tooling` when the `git` convention module is installed. External `uses:` references in GitHub workflow/composite-action YAML must resolve to full 40-character commit SHAs; repository-local actions are excluded.
