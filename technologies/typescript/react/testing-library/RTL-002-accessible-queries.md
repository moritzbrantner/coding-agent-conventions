# RTL-002 — Prefer accessible queries

**Status:** Accepted

In component tests, find elements through roles, labels, names, and visible text whenever possible. Use a dedicated test marker only when no stable user-facing query exists.
