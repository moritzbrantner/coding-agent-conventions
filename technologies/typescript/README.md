# TypeScript conventions

Rules in this directory use the `TS-*` prefix and apply to TypeScript code independent of React, Next.js, or another framework unless the rule says otherwise.

Put a rule here when it is fundamentally about TypeScript types, modules, language constructs, compiler behavior, or TypeScript-level API design.

Do not duplicate TypeScript rules in framework directories merely because those frameworks use TypeScript.
