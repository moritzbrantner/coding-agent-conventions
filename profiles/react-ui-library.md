# Profile — react-ui-library

**Status:** Accepted
**Extends:** `react-vite-library`

## Stack

- `@moritzbrantner/ui`

## Convention branches

- `technologies/typescript/react/moritzbrantner-ui/`

The React library and tooling branches come from `react-vite-library`; parent technology scopes are inherited through the catalog rather than repeated here.

## Library boundary

This profile is for the reusable `@moritzbrantner/ui` design-system package. Product workflows, routing, authorization, server state, persistence, and product copy remain in consuming applications. Repository-local instructions own its public tiers, generated registry workflow, validation commands, and release boundary.
